<?php
/**
 * 設定ファイルの雛形。
 *
 *   このファイルをコピーして「config.php」という名前で保存し、
 *   レンタルサーバーの管理画面で確認したMySQLの情報に書き換えてください。
 *
 *   cp config.sample.php config.php
 *
 * config.php は絶対に公開リポジトリへ入れないでください（.gitignore 済み）。
 */

return [
    // ---- MySQL 接続情報（レンタルサーバーの管理画面で確認できます）----
    'db_host'     => 'localhost',   // 「MySQLサーバー名」「ホスト名」など
    'db_port'     => 3306,
    'db_name'     => '',            // データベース名
    'db_user'     => '',            // データベースのユーザー名
    'db_password' => '',            // データベースのパスワード

    // ---- 画像の共有 ----
    // true にすると、撮影したレシート画像もサーバーへ保存し、PC・スマホの両方で見られます。
    'store_images' => true,
    // 画像1枚あたりの上限（バイト）。既定は2MB。
    'max_image_bytes' => 2 * 1024 * 1024,

    // ---- 追加の合言葉（任意）----
    // 空文字なら未使用。設定した場合、アプリの「設定 > サーバー」にも同じ値を入れます。
    // ディレクトリにBasic認証をかけている場合は、空のままでも構いません。
    'access_token' => '',
];
